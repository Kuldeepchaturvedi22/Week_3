package com.ComparingDifferentDataStructuresSearching;

import java.util.TreeSet;

public class TreeSetSearch {
    public static boolean search(TreeSet<Integer> set, int key) {
        // Check if the set contains the specified key
        return set.contains(key);
    }
}